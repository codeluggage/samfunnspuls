# Upstream Context

This directory stores machine-readable context pulled from the upstream DesignSystem repo or its published Storybook artifacts.

Tracked files in this directory are refresh targets, not hand-edited source files.
