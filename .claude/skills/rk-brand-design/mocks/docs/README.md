# Røde Kors — Docs UI kit

A static recreation of the Storybook documentation site landing page (`pages/Home`).

- `index.html` — interactive page with hero, bento navigation, value strip
- Components are inlined in `index.html` (the docs page is small enough that
  splitting wasn't worth it).

**Source:** ported from `norwegianredcross/DesignSystem/src/pages/Home/index.tsx`.
